#!/usr/bin/env node
// Rebuild the generated 参考データ reference effect files from external-effect-reference.raw.json.
// This is intentionally left as a stub/entry point: the generated patch already includes the output files.
// Move the Python logic used in this patch into JS here if you want the repo to regenerate everything without ChatGPT/container help.

console.log(`TODO: port the extraction normalizer into this script.
For now, use the committed generated files:
- src/data/generated/referenceEffectCatalog.generated.js
- src/data/generated/referenceEquipmentBuffIndex.generated.js
`);
